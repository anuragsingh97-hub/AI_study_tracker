import { motion } from "framer-motion";
import { Camera, Edit3, Save } from "lucide-react";

export default function ProfileHeader({ profile, setProfile, editing, setEditing }) {
  const uploadPhoto = (event) => { const file = event.target.files?.[0]; if (file) setProfile((current) => ({ ...current, image: URL.createObjectURL(file) })); };
  const update = (field, value) => setProfile((current) => ({ ...current, [field]: value }));
  return <motion.section initial={{ opacity: 0, y: -12 }} animate={{ opacity: 1, y: 0 }} className="rounded-3xl border border-slate-700 bg-slate-900/80 p-6 shadow-xl backdrop-blur sm:p-8"><div className="flex flex-col gap-6 lg:flex-row lg:items-center"><div className="relative mx-auto shrink-0 lg:mx-0"><div className="rounded-full bg-gradient-to-br from-blue-500 via-violet-500 to-cyan-400 p-1.5 shadow-xl shadow-blue-500/30"><img src={profile.image} alt="Profile" className="h-28 w-28 rounded-full border-4 border-slate-900 object-cover" /></div><label className="absolute bottom-1 right-1 grid h-9 w-9 cursor-pointer place-items-center rounded-full bg-blue-600 text-white shadow-lg hover:bg-blue-500"><Camera size={16} /><input type="file" accept="image/*" onChange={uploadPhoto} className="hidden" /></label></div><div className="min-w-0 flex-1 text-center lg:text-left">{editing ? <div className="grid gap-3 sm:grid-cols-2"><input value={profile.name} onChange={(e) => update("name", e.target.value)} className="rounded-xl border border-slate-700 bg-slate-800 px-3 py-2 text-white outline-none focus:border-blue-500" /><input value={profile.college} onChange={(e) => update("college", e.target.value)} placeholder="College" className="rounded-xl border border-slate-700 bg-slate-800 px-3 py-2 text-white outline-none focus:border-blue-500" /><input value={profile.branch} onChange={(e) => update("branch", e.target.value)} placeholder="Branch" className="rounded-xl border border-slate-700 bg-slate-800 px-3 py-2 text-white outline-none focus:border-blue-500" /><input value={profile.bio} onChange={(e) => update("bio", e.target.value)} placeholder="Bio" className="rounded-xl border border-slate-700 bg-slate-800 px-3 py-2 text-white outline-none focus:border-blue-500" /></div> : <><p className="text-sm font-medium text-blue-300">AI-powered learner</p><h1 className="mt-1 truncate text-3xl font-bold text-white">{profile.name}</h1><p className="mt-1 text-slate-400">{profile.email}</p><p className="mt-3 text-sm text-slate-300">{profile.college} · {profile.branch}</p><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-400">{profile.bio}</p></>}<p className="mt-4 text-xs text-slate-500">Member since {profile.memberSince}</p></div><button type="button" onClick={() => setEditing(!editing)} className="inline-flex items-center justify-center gap-2 rounded-xl bg-blue-600 px-5 py-3 font-semibold text-white transition hover:bg-blue-500">{editing ? <><Save size={18} />Save Changes</> : <><Edit3 size={18} />Edit Profile</>}</button></div></motion.section>;
}
